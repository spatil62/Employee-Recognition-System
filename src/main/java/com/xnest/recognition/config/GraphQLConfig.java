package com.xnest.recognition.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class GraphQLConfig {
    // Spring Boot 3 handles config automatically — you don’t need much here unless customizing scalars or subscriptions.
}
